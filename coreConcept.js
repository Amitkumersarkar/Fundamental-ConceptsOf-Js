// length, push

const numbers = [89, 30, 20, 10];
numbers[0] = 50;

// for loop
for (let i = 0; i < numbers.length; i++) {
    const numbers = numbers[i];
    console.log(numbers);
}

// function 
function multiply(num1, num2) {
    const result = num1 * num2;
    return result;
}